_schema-version: "2.0.0"
ID: com.sap.sample.mta
version: 0.1.0

modules:
  - name: module-1
    type: java.tomee
    parameters:
      idle-hosts: [foo, bar]
      idle-domain: localhost

  - name: module-2
    type: java.tomee
    parameters:
      idle-hosts: [baz, qux]
      idle-domain: localhost
