_schema-version: "2.0.0"
ID: com.sap.xs2.test
version: 0.1.0

modules:
  - name: foo
    type: foo
    requires:
      - name: bar
        parameters:
          config:
            baz: baz
            qux: qux
    parameters:
      route-path: /test

resources:
  - name: bar
    type: bar
