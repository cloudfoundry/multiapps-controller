_schema-version: "2.0.0"
ID: com.sap.test
version: "1.0"

modules:
  - name: foo
    type: foo
    parameters:
      host: .com.sap.test.foo

  - name: bar
    type: bar
    parameters:
      domain: com.sap.test.foo

resources:
  - name: baz
    parameters:
      host: .com.sap.test.foo
